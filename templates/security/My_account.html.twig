{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

 {% block body %}

     <h1>Salut👋 {{ app.user.email }}!</h1>

     {% for role in app.user.roles %}

         <h2> Statut : {{ role }} </h2>

     {% endfor %}

     <h2>Liste des séries ajoutées :</h2>

     {% for article in userArticles %}

         <ul>
             <li>
                 <img class="align-self-start mr-3 " width="200px" src="{{article.poster}}" alt="{{ article.title }} poster">
                 <h2>Catégorie : {{ article.plateform.name }}</h2>
                 <p>{{ article.title}}: {{ article.description }}</p>

             </li>
         </ul>

     {% endfor %}

 {% endblock %}
